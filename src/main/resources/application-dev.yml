spring:

  datasource:
    url: jdbc:postgresql://postgres:5432/giveat_dev #${DB_URL_DOCKER}
    username: admin #${DB_USER_DEV:admin}
    password: 1234 #${DB_PASSWORD_DEV:1234}

  jpa:
    hibernate:
      ddl-auto: update

  security:
    jwt:
      expiration: 1 #d
      refresh-token:
        expiration: 15 #d
  mail:
    username: ${SMTP_CLIENT_DEV}
    password: ${SMTP_PW_DEV}